# @mcp-z/cli - Example Test Configuration
# Copy this file to .env.test and fill in your actual credentials

# ============================================================================
# Test Environment
# ============================================================================
NODE_ENV=test

# ============================================================================
# Manual Testing Configuration
# ============================================================================
# Set to 'true' to enable tests requiring browser-based OAuth flows
# These tests require human interaction (clicking "Accept" in browser consent screens)
# and cannot run in CI/CD environments without human intervention
#
# Default: false (skips OAuth integration tests for CI/CD compatibility)
#
# When TEST_INCLUDE_MANUAL=false (default):
#   - Automated tests run (bearer token, internal DCR, command tests)
#   - External OAuth tests are skipped
#
# When TEST_INCLUDE_MANUAL=true:
#   - All tests run, including those requiring browser OAuth consent
#   - External DCR tests with Todoist (if DCR-enabled)
#   - Manual verification tests
#
TEST_INCLUDE_MANUAL=false

# ============================================================================
# Todoist Integration Test Configuration
# ============================================================================
# NOTE: Todoist tests run when TEST_INCLUDE_MANUAL=true
# Tests verify RFC 9728 + RFC 8414 discovery against real Todoist server

# Todoist MCP Server URL (default: https://ai.todoist.net/mcp)
# TODOIST_MCP_SERVER_URL=https://ai.todoist.net/mcp

# ============================================================================
# Test Data Configuration
# ============================================================================
# Temporary directory for test artifacts (default: .tmp/)
# All test files are isolated in this directory per QUALITY.md T8
# TEST_TMP_DIR=.tmp
